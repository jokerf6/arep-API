model City {
  id        Int       @id @default(autoincrement())
  name      Json
  deletedAt DateTime? @map("deleted_at")

  @@map("city")
}

model Nationality {
  id        Int       @id @default(autoincrement())
  name      Json
  deletedAt DateTime? @map("deleted_at")

  @@map("nationality")
}
