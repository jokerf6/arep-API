model BankAccount{
    id Int @id @default(autoincrement())
    accountName Json
    bankId Int
    phone String
    ibn String
    Bank   Bank @relation(fields: [bankId], references: [id], onDelete: Cascade)
    @@map("bank_accounts")
}